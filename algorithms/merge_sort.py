def merge_sort(arr):
    """
    Merge Sort Algorithm.
    Params:
        arr (list[int]): Input list of integers
    Returns:
        tuple: (sorted_list, comparisons, operations)
    Note:
        Merge Sort recursively divides the list, sorts each half,
        and merges them back together.
        Track comparisons and merge operations.
    """
    pass
